Config = Config or {}

Config.StorageContainers = {}

-- Editable

Config.CoreExport = exports['qb-core']:GetCoreObject() -- New Qbus

Config.EstateJob = "realestate" -- Job to place Storage

Config.StashSlots = 15 -- Slots for stash (Future Upgradeable stashes)
Config.MaxStashWeight = 1000000 -- Weight for stash (Future Upgradeable stashes)

